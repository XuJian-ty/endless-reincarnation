# 《无尽轮回》项目介绍说明书

## 文档说明

这份文档不是立项阶段的需求说明书，也不是给程序内部看的简略开发笔记，而是站在“项目已经做出当前可运行版本”这一前提下，对《无尽轮回》进行一次完整介绍。

它主要服务三个用途：

1. 方便向别人详细介绍这个项目目前已经做到了什么；
2. 作为后续撰写毕业论文时的项目事实依据；
3. 作为后面继续完善项目时的阶段性整理文档。

因此，本文档更关注“现在这个项目真实有什么、它是怎么跑起来的、哪些地方已经形成闭环、哪些地方还只是基础版本”，而不是去列一个理想化的未来需求清单。

---

## 一、项目概况

《无尽轮回》是一个基于 Unity 开发的第三人称 3D 动作游戏项目。它的核心不是纯随机肉鸽，也不是单纯做一个能走能打的动作 Demo，而是尽量搭出一个完整的单机动作游戏闭环：玩家可以从主界面进入游戏，创建或读取存档，进入关卡后进行探索和战斗，获得掉落和成长资源，解锁技能、装备武器、选择 Buff，清理守卫者并触发最终 Boss，击败后继续推进流程或留在当前关卡继续游玩。通关后，还能从菜单进入战斗回忆模式，对已击败过的 Boss 进行纯挑战。

从项目实际完成情况来看，它已经不是“只有一套角色动作”的阶段，而是具备了比较完整的多系统协同结构。当前工程中已经接通的主要内容包括：

- 主界面背景、主菜单、起名、新建存档、读取存档、加载流程；
- 关卡启动、玩家初始化、HUD 初始化、关卡内菜单与暂停；
- 角色移动、转向、跳跃、二段跳、闪避、近战连击、空中攻击、下落攻击、远程姿态与射击；
- 主动技能、被动技能、技能槽、技能变异、技能时间轴效果；
- 敌人 AI、近战与远程小怪、精英、守卫者、Boss、最终 Boss 决战；
- 宝箱、掉落、拾取、对话、商店、背包、装备、药剂、仙露；
- Buff 三选一、技能树、按键重绑定、声音设置；
- 本地 JSON 存档、Checkpoint 回滚、死亡选择、仙露复活；
- 战斗回忆模式与返回原场景流程。

如果只用一句话来概括这个项目当前的定位，那它更接近于：

**一个以第三人称动作战斗为核心，加入关卡推进、随机 Buff、技能成长、资源管理和存档恢复机制的单机动作游戏原型。**

---

## 二、项目当前的整体完成形态

### 2.1 它现在已经不是单一场景演示

当前项目已经包含多个独立场景，并且这些场景之间存在明确分工，而不是只有一个测试场景把所有内容塞进去。

当前已加入构建设置的场景包括：

- `MainMenu`：主菜单场景；
- `Level_1` ～ `Level_5`：五个关卡场景；
- `BattleMemoryScene`：战斗回忆场景；
- `FinalBossDuelScene`：最终 Boss 决战场景。

这意味着项目当前的结构已经从“一个场景里演示角色动作”变成了“多个场景承载不同流程角色”的状态。主菜单负责进入游戏和管理存档，普通关卡负责探索、战斗、成长和 Boss 流程，战斗回忆负责通关后的纯挑战，最终 Boss 决战场景则专门承担最终 Boss 的战斗过程。

### 2.2 它也不是只做了动作系统

从代码结构来看，项目已经明显分成了几个层次：

- `GameFlow` 负责主流程、关卡启动、导演、掉落、商店交互、战斗回忆与最终 Boss 决战等；
- `Presentation` 负责玩家动作、状态机、技能时间轴、敌人控制、分身、伤害处理等；
- `UI` 负责主界面、背包、技能树、商店、Buff 选择、菜单、按键配置、死亡面板等；
- `Data` 负责关卡配置、敌人配置、武器配置、技能配置、Buff 配置、按键配置等 ScriptableObject 资源；
- `Domain` 负责玩家数据模型、属性、装备与背包结构；
- `Saving` 负责本地 JSON 存档结构和读写。

也就是说，当前项目的重点已经不是“再多补几个动作状态”，而是“怎么让动作系统、成长系统、场景流程和存档系统一起工作”。这也是它和单纯动作演示项目最不一样的地方。

---

## 三、从玩家视角看完整游玩流程

这一部分直接从玩家能看到、能操作到的内容来说明项目当前到底是怎么跑的。

### 3.1 进入游戏

项目启动后，不会立刻把主菜单按钮全部摆出来，而是先显示主界面背景和“点击任意键继续”的提示。玩家按任意键或点击鼠标后，才真正进入主菜单。

主菜单目前有四个明确入口：

- 新的游戏；
- 加载存档；
- 声音设置；
- 退出游戏。

如果玩家选择新的游戏，会先进入起名面板。输入名字并确认后，系统会立刻创建一份真实存档，然后显示加载面板，并异步进入对应关卡。这里不是先临时进关再补存档，而是先有一份真正的存档，再用这份存档进入关卡。

如果玩家选择加载存档，则会进入存档列表。存档列表目前已经支持：

- 显示所有已存在的存档；
- 每条存档显示玩家名和当前关卡；
- 左键直接读取存档；
- 右键弹出存档菜单，对该存档进行重置或删除。

这一部分和很多毕设项目里“只有三个固定槽位”的做法不一样，当前工程是按 `id` 管理存档，数量不限。

### 3.2 进入关卡

进入关卡后，系统会做一整套初始化，而不只是把玩家放进场景里。

当前关卡启动过程里，会依次处理这些内容：

- 接入当前运行中的存档数据；
- 初始化玩家模型和角色实例；
- 放置玩家位置和朝向；
- 绑定第三人称相机；
- 注册死亡选择处理器；
- 显示玩家 HUD、最终 Boss 血条叠层、战斗飘字层；
- 如果本关需要重新选择 Buff，则弹出 Buff 三选一面板；
- 如果当前存档中已经存在关卡快照，则恢复敌人、掉落物、商店、生成器和导演状态。

这一步比较关键，因为它说明这个项目不是“每次进关都从零开始”，而是已经支持根据当前存档状态恢复关卡运行结果。

### 3.3 在关卡中的主要行为

玩家进入关卡后，主要可以做的事情包括：

- 移动、跳跃、二段跳、闪避；
- 切换近战与远程形态；
- 进行普通攻击、蓄力攻击、空中攻击和下落攻击；
- 释放主动技能；
- 开启宝箱；
- 拾取地面掉落；
- 与胡桃对话后进入商店；
- 打开背包；
- 打开技能树；
- 打开关卡菜单、按键配置和声音设置；
- 清理守卫者并触发 Boss 流程。

这也是为什么这个项目不能只用“动作系统”来概括。对玩家来说，它现在已经是一个“边战斗边成长边推进流程”的项目，而不是纯粹的操作测试场景。

### 3.4 死亡、复活与回滚

玩家死亡后，不是立即 Game Over，也不是简单原地复活，而是会弹出死亡选择面板。当前有两种结果：

- 消耗仙露复活；
- 放弃当前进度。

如果选择仙露复活，系统会：

1. 回滚到最近一次 Checkpoint；
2. 难度加一；
3. 移除当前关卡本次选择的 Buff；
4. 重新标记本关需要重新三选一 Buff；
5. 重新加载当前关卡。

如果选择放弃，则会删除当前存档并返回主菜单。

这一整套逻辑决定了项目中的“死亡”不是一个单纯的表现事件，而是和存档、成长、难度、关卡流程强绑定的。

### 3.5 Boss、结算与战斗回忆

关卡内的普通推进流程不是“清怪后直接结算”，而是先清理守卫者。守卫者全部被击杀后，`LevelDirector` 会弹出 Boss 即将降临的提示，然后触发最终 Boss 流程。

值得注意的是，当前工程里的最终 Boss 不是简单在普通关卡里原地刷新后打完就结束，而是会切到专门的 `FinalBossDuelScene` 决战场景中进行挑战。击败后会显示 Boss 结果面板，玩家可以选择：

- 重打本关；
- 继续推进。

如果还有下一关，就进入下一关；如果已经没有下一关，则记录通关状态并返回原场景。

当当前存档已经处于通关状态后，关卡菜单中会额外出现“战斗回忆”按钮。进入后，玩家可以从列表里选择已解锁的 Boss，切到独立的战斗回忆场景进行纯挑战。该模式：

- 不发奖励；
- 不改当前进度；
- 失败后不会删除存档；
- 胜负结束后都会返回原场景。

这一块很适合当成项目的一个辨识度较高的附加模式来看，因为它已经不是简单的“再打一遍 Boss”，而是独立成了一条特殊流程。

---

## 四、项目的重点内容

下面几部分是当前项目里最值得重点介绍的内容。它们不是平均重要的，我会把真正的重点写得更细一点。

### 4.1 重点一：角色动作与战斗闭环

如果只从项目价值来看，当前工程里最核心的还是角色动作和战斗系统。因为别的成长、存档、交互和流程，最后都要围绕战斗成立。

#### 4.1.1 基础动作已经比较完整

当前玩家控制不是只做了一个待机加普攻，而是有一整套比较完整的动作集合：

- 待机；
- 走路与跑步；
- 地面跳跃；
- 空中二段跳；
- 普通下落；
- 闪避；
- 近战四段普攻；
- 蓄力开始、蓄力循环、蓄力释放；
- 远程瞄准与射击；
- 空中攻击；
- 下落攻击开始、下落攻击循环、下落攻击着陆；
- 主动技能状态；
- 受击硬直；
- 死亡状态。

这些动作不是平铺的，而是由玩家状态机统一组织。输入不会直接改动画，而是先解析成动作，再根据当前状态判断是立刻打断、缓存还是忽略。

#### 4.1.2 输入不是硬写死的，而是有动作路由和预输入

当前项目里，输入处理和动作执行之间并不是硬绑定关系。它中间还隔了两层比较重要的逻辑：

一层是**动作路由**。它负责把“攻击”“闪避”“技能槽位技能”等输入，映射成统一的动作标识。  
另一层是**状态机策略**。它负责判断当前动作是否允许立刻执行，还是应该缓存起来等当前动作结束。

这样做的意义是很明显的：

- 普攻连击、闪避打断、技能打断这些逻辑会更好组织；
- 基础动作和技能都可以走同一套状态判断框架；
- 新增技能时，不必为每个技能重新写一套单独的输入链。

#### 4.1.3 近战和远程不是两套割裂系统

当前项目的玩家不是“只能近战”或者“只能远程”，而是支持双形态切换。玩家数据里会同时记录：

- 当前近战武器；
- 当前远程武器；
- 当前攻击形态。

切换形态后，基础动作和技能会通过配置映射到对应的动作 ID。这样近战和远程虽然表面表现不同，但底层仍然走统一的动作与状态结构。

这也是当前项目一个比较好的点：它没有为了做双武器形态就把角色控制拆成两套完全分离的系统。

#### 4.1.4 战斗计算已经形成闭环

项目当前的伤害计算不是简单固定扣血，而是已经接入了完整的属性体系。

角色当前战斗强度会受到这些内容共同影响：

- 基础等级属性；
- 当前装备武器；
- Buff 修正；
- 被动技能修正；
- 当前生命和法力；
- 当前攻击形态。

伤害计算里已经考虑了这些因素：

- 攻击；
- 防御；
- 暴击率；
- 暴击伤害；
- 吸血；
- 增伤；
- 减伤。

命中后，系统还会同时处理：

- 伤害值变化；
- 吸血回复；
- 飘字显示；
- 命中特效与音效；
- 命中停顿；
- 受击控制。

也就是说，从“出手”到“伤害生效”再到“视觉反馈”，这条战斗链现在已经接通了。

### 4.2 重点二：技能时间轴与技能配置体系

这一块是当前项目里最值得写细的部分之一。因为它不是一个简单的“技能有冷却、有伤害”的配置表，而是已经形成了作者化程度比较高的技能体系。

#### 4.2.1 技能不是只靠状态名区分

当前工程里，技能相关配置至少拆成了几层：

- 玩家动作及技能配置库；
- 技能效果库；
- 被动技能效果库；
- 动画库和 Animator 触发；
- 技能树中的解锁、装槽和变异关系。

一个主动技能的实际执行，不是只靠某一个类名决定，而是会经过：

1. 输入解析到技能槽位动作；
2. 根据配置找到技能条目；
3. 根据技能条目解析具体使用哪一份技能效果定义；
4. 交给技能时间轴去逐帧执行；
5. 再由效果执行器把伤害、命中、特效、音效等效果放出来。

这样的好处是，新技能的接入不再完全依赖“额外写一堆专门代码”，而是可以尽量在数据和资源层面完成。

#### 4.2.2 时间轴技能效果是真正的重点能力

当前技能效果不是一条“释放后造成 X 伤害”的黑盒，而是能拆成不同时间点、不同类型的事件。

已接入的技能效果类别包括：

- 伤害事件；
- 命中停顿；
- 物理位移或控制；
- 属性效果；
- 特效；
- 音效；
- 镜头相关效果。

这意味着一个技能从开始到结束不是一个整体动作，而是一串按时间触发的事件集合。  
这类结构特别适合动作游戏，因为动作、命中和表现之间的协调，本来就很依赖时间轴。

#### 4.2.3 技能变异已经和技能树接通

当前项目里的技能不是“解锁后永远只有一个版本”。技能树运行时面板已经支持：

- 节点解锁；
- 主动技能拖拽装槽；
- 技能变异切换；
- 当前变异结果写回玩家数据。

也就是说，技能变异不是停留在设计设想里，而是已经接入了实际运行逻辑。玩家当前选择的变异会记录在存档里，并影响后续战斗时解析到的具体技能效果定义。

这一点对论文和项目介绍都很重要，因为它说明当前工程不是“只有技能数量”，而是已经开始做技能内容层的变化。

### 4.3 重点三：敌人、守卫者、Boss 与遭遇流程

如果玩家只会打木桩，那动作系统再丰富也撑不起一个游戏项目。所以敌人系统是第二个必须写重的部分。

#### 4.3.1 敌人不是一个统一模板

当前项目中的敌人至少区分成这些层级：

- 近战小怪；
- 远程小怪；
- 精英怪；
- 守卫者；
- Boss；
- 最终 Boss。

而在更底层的实现里，Boss 还不是只有一个。当前工程的 Boss 生成和战斗回忆列表，都是建立在 Boss 变体和 `enemyId` 的基础上的。

#### 4.3.2 敌人 AI 用的是“感知 + 行为树 + 执行”这条链

当前敌人 AI 不是单脚本里写一串 if-else。它更接近一种分层结构：

- 感知层：负责发现目标、判断是否有即时威胁、保留目标记忆；
- 决策层：由行为树决定当前该巡逻、规避、惩罚、战斗、搜索还是继续巡逻；
- 执行层：把意图交给敌人控制器和移动/战斗模块去落实。

当前默认行为树的主干大致是：

受击优先 → 规避威胁 → 抓惩罚窗口 → 常规战斗 → 搜索目标 → 巡逻。

这说明当前工程里的敌人并不是只会“看到玩家就直线追上去”，而是已经开始做简单的战术分支。

#### 4.3.3 遭遇流程不是静态摆怪

关卡中的敌人不是完全靠场景手工摆放。当前工程已经有：

- 关卡全局生成器；
- 场景局部生成器；
- 关卡导演；
- 运行时快照恢复。

这意味着敌人的出现、关卡推进、守卫者清理、Boss 降临，都是流程化管理的，而不是纯静态设计。

守卫者清空后，`LevelDirector` 会接手后续流程，显示 Boss 即将降临的提示，再根据当前进度拉起 Boss 流程。这也是为什么当前关卡不是“玩家随便找个 Boss 打完就结束”，而是有明确阶段结构。

#### 4.3.4 最终 Boss 和战斗回忆是两条特殊流程

当前普通关卡里的最终 Boss，会切换到 `FinalBossDuelScene` 进行决战。  
通关后的战斗回忆，则切换到 `BattleMemoryScene` 进行纯挑战。

这两个场景都有一个共同点：都会在进入时清掉普通关卡里的宝箱、掉落物、商店、生成器和导演对象，只保留玩家与要挑战的 Boss，从而形成独立的战斗环境。

这一做法的好处是流程更清楚，缺点是切场景意味着表现和上下文管理要更谨慎。但从现在的项目形态看，这套流程已经基本搭出来了。

### 4.4 重点四：成长、资源和长期推进

如果说动作和敌人决定了“打起来怎么样”，那成长和资源系统决定的就是“打完以后有什么留下来”。

#### 4.4.1 背包不是临时列表，而是统一 50 格

当前玩家数据模型中，背包就是统一 50 格，不再区分“装备栏是一个系统、药水栏是另一个系统、材料栏又是一个系统”。

每一格只能是三种情况之一：

- 空；
- 一把武器；
- 一类可堆叠物品。

金币和天赋点不占格，单独存数量。这样做的好处是：

- 存档结构清楚；
- 背包 UI 和数据模型对应关系直接；
- 武器和普通物品的买卖、拾取、掉落可以走更统一的流程。

#### 4.4.2 武器已经是实例化数据

当前武器不是“只记录一个武器类型”，而是会生成真正的武器实例。实例里会保存：

- 武器 ID；
- 武器类型；
- 品质；
- 随机出的属性结果。

这点很重要，因为它意味着项目里的武器已经不是“查表即得”的静态配置，而是从配置范围里 Roll 出来的运行时结果。不同品质会开放不同的属性范围，低品质没有的字段保持为 0。

#### 4.4.3 Buff 和技能树都已经参与长期成长

当前项目里，Buff 不是一次性只在当前局内生效、关卡切走就什么都不剩的那种处理方式。它会写入玩家数据和存档，属于成长结果的一部分。更准确地说，是“当前关本次选择的那个 Buff”在复活或重打本关时会被移除并要求重选，而不是整个 Buff 系统都只是临时效果。

技能树则更偏永久成长，负责：

- 解锁技能；
- 解锁被动；
- 维护技能槽位；
- 记录变异选择。

从玩家体验上说，当前成长系统可以理解成两层：

- **技能树**偏计划型成长；
- **Buff**偏流程中的追加成长。

这两者当前都已经和战斗系统接上了，而不是停留在界面摆设阶段。

### 4.5 重点五：存档、Checkpoint 和流程恢复

这也是当前项目非常值得写的一块，因为它已经超出“只存等级和金币”的水平了。

#### 4.5.1 存档不是只记录玩家数值

当前存档根结构里，除了玩家等级、血蓝、背包和武器，还会记录：

- 当前关卡和难度；
- 当前攻击形态；
- 已获得 Buff；
- 已解锁技能；
- 技能槽位映射；
- 技能变异选择；
- 已击败 Boss 列表；
- 通关状态；
- 关卡内 BGM 和音效设置；
- 按键重绑定结果；
- Checkpoint；
- 是否待选 Buff；
- 当前关卡快照。

这说明现在的存档已经承担起“恢复整局状态”的作用，而不仅仅是做一个角色属性备份。

#### 4.5.2 关卡快照是这个项目很重要的一层

当前存档里的 `LevelSnapshot` 已经覆盖了不少运行时内容：

- 玩家在场景中的位置和朝向；
- 敌人快照；
- 已开宝箱；
- 地面掉落物；
- 商店库存；
- 全局/局部生成器状态；
- 导演状态。

这意味着退出游戏再读档时，系统不是简单回到“这关开局”，而是尽量恢复到你离开前的关卡状态。

#### 4.5.3 死亡回滚和继续推进都依赖这套数据结构

Checkpoint、仙露复活、Boss 后推进下一关、重打本关，这些流程之所以能工作，靠的就是 `RunData` 和 `LevelSnapshot` 这套结构已经比较完整。

如果没有这一层，当前项目里很多流程都会退化成“死亡就回主菜单”或者“只能从固定点继续”，而不是现在这种能区分：

- 退出保存；
- 正常读档；
- 仙露复活回滚；
- Boss 后重打本关；
- Boss 后进入下一关；
- 通关后进入战斗回忆。

---

## 五、配套系统与界面

上面几节是项目里的核心内容，下面这些虽然不是最核心的亮点，但对于项目完整性来说同样重要。

### 5.1 主界面与加载表现

主界面当前已经分成了：

- 主界面背景面板；
- 主菜单面板；
- 起名面板；
- 存档列表面板；
- 主菜单声音设置面板；
- 加载面板。

这个拆分方式比较合理，因为“背景”和“主菜单按钮区”并不是绑死在一起的。这样切场景时也能复用主界面背景作为过渡层。

### 5.2 关卡菜单和暂停

玩家在关卡中可以通过菜单打开：

- 继续游戏；
- 关卡声音设置；
- 退出并保存；
- 战斗回忆（仅通关后显示）。

菜单、背包、技能树、按键配置这些面板并不是各自单独暂停，而是通过 `GameplayUIInputBridge` 统一控制：

- 哪些面板会阻断玩法输入；
- 哪些面板打开后要暂停游戏；
- 关闭后如何恢复时间和鼠标状态。

这部分虽然不是最炫的功能，但很影响整个项目的可用性。当前工程在这里已经做得比较完整。

### 5.3 对话、商店与交互

胡桃商店的流程不是“走过去自动弹商店”，而是先通过对话面板进入多轮对话，再在合适的选项处进入商店。

商店本身也不是一张死图，而是会根据当前商店实例的商品数据动态生成并刷新：

- 商品图标；
- 名称；
- 价格；
- 剩余数量；
- 售罄状态。

这一部分加上宝箱交互和地面掉落拾取，实际上已经构成了一个比较完整的“场景交互链”。

### 5.4 按键配置

当前按键配置不是手工在预制体里摆死一堆行，而是根据按键配置资源动态生成。  
而且按键重绑定不是全局设置，而是按当前存档独立保存。

这使得项目从“一个演示项目”更像“一个真正可玩的单机项目”，因为它开始照顾不同玩家的输入习惯。

### 5.5 声音设置

项目当前把主菜单声音设置和关卡声音设置分开处理：

- 主菜单 BGM 由主界面面板控制；
- 关卡内 BGM 和音效由关卡菜单中的声音设置控制；
- 两者的作用范围不一样。

这不是最重的功能，但作为项目整体完整性的一部分是成立的。

---

## 六、当前项目中的一些特色点

这一部分不再从系统拆分角度写，而是直接说这个项目当前有哪些比较值得拿出来讲的点。

### 6.1 动作和成长不是割裂的

很多项目会把动作系统和成长系统做成两张皮：战斗时一套逻辑，背包和技能树又是另一套逻辑，彼此只是表面连接。  
当前工程虽然还有进一步解耦的空间，但至少在主链上已经能做到：

- 武器属性会真实影响战斗；
- Buff 会真实影响战斗表现和角色数值；
- 技能树解锁和变异会真实影响技能释放结果；
- 存档会真实影响后续进入关卡时的状态。

### 6.2 分身、重影和范围扩大这类特殊 Buff 已经落地

这类 Buff 不只是“属性 +10%”那种简单数值修正，而是会真正改变战斗表现：

- 分身会生成实体分身并跟随玩家协助战斗；
- 重影会在时间轴层面追加延迟事件；
- 范围扩大则会改伤害范围和表现范围。

这说明项目里的 Buff 已经开始从纯属性层扩展到机制层。

### 6.3 战斗回忆模式让项目不只剩主线推进

虽然战斗回忆目前还是基础版本，但它已经给项目增加了一条比较清楚的“通关后还能做什么”的内容线。  
对毕设项目来说，这种额外模式很有价值，因为它能明显证明项目不是只做了“一条打通就结束”的流程。

---

## 七、哪些内容已经完成，哪些内容还需要继续打磨

### 7.1 已经形成基础闭环的部分

以下这些内容，当前已经不是“只有半截”，而是形成了可以跑通的基础闭环：

- 主菜单、新建游戏、读档与加载；
- 关卡启动、玩家初始化与 HUD；
- 第三人称移动、跳跃、闪避、近战、远程和主动技能；
- 敌人 AI、守卫者清理与 Boss 流程；
- 背包、宝箱、拾取、对话和商店；
- 技能树、技能槽和技能变异；
- Buff 三选一；
- 存档、Checkpoint、仙露复活；
- 关卡菜单、按键配置、声音设置；
- 战斗回忆与最终 Boss 决战场景。

### 7.2 仍然偏基础版本、还需要继续完善的部分

这些内容虽然已经存在，但目前还不算“完成得很成熟”：

- 关卡内容量和探索密度还不够高；
- 第 3～5 关更多还是基础或占位场景；
- 技能树和战斗回忆的界面表现仍有继续打磨空间；
- 数值平衡和战斗节奏还可以继续优化；
- 场景演出、环境布置、音效细节还不算丰富；
- 模块之间仍然有一些耦合偏重的地方；
- 自动化测试和更系统的调参工具还不够完善。

### 7.3 当前不应该夸大的部分

为了避免后面写论文时把项目写得太满，这里也要把几个边界说清楚：

- 它不是一个内容量已经很大的商业游戏；
- 它不是一个纯肉鸽项目；
- 它不是一个联机项目；
- 它虽然有战斗回忆和双形态战斗，但整体上仍然属于“完成了核心闭环的毕设级动作游戏原型”。

这个定位是健康的，也更接近它现在的真实状态。

---

## 八、旧《功能需求说明书》与当前项目实际相比，哪些地方已经过时

这一节是专门加上的，方便后面你回头处理旧文档时有个对照。

### 8.1 过时或不准确的内容

1. **项目标题和整体定位偏旧。**  
旧文档标题仍然是“3D 肉鸽项目”，但从当前工程实际看，它已经更接近“以第三人称动作战斗为核心，加入成长和随机选择机制的动作游戏项目”，不能再只用“肉鸽项目”概括。

2. **最终 Boss 流程写得不够准确。**  
旧文档里把最终 Boss 描述得更像是在普通关卡里直接降临并完成整套结算，但当前实际实现中，会进入专门的 `FinalBossDuelScene` 进行决战。

3. **战斗回忆失败后的流转写错了。**  
旧文档提到“失败后自动重开”，但当前代码里的实际行为是：战斗回忆胜负结束后都会返回原场景，而不是在回忆场景里无限自动重开。

4. **Buff 数量和内容已经过时。**  
旧文档里写的是 13 种 Buff，还提到“远程每次多发一发子弹”的 Buff；但当前 `BuffConfigSO` 中实际接入的是 12 种 Buff，而且没有这条多发子弹 Buff。

5. **玩家基础属性项数写旧了。**  
旧文档中把玩家基础属性写成 12 项，但当前 `Stats` 实际已经包含 13 项，其中多了减伤这一项。

6. **某些未来规划和当前已实现内容混写在了一起。**  
旧文档中有些地方一边在描述“当前已经实现”，一边又在夹带“后续想做什么”，读起来不容易分清哪些是事实、哪些是规划。

### 8.2 结构上为什么建议重构

旧需求说明书本身并不是没用，它在“列需求”和“约束开发边界”这件事上是成立的。问题主要是它现在已经同时承担了几种不同角色：

- 需求清单；
- 当前实现说明；
- 数据设计笔记；
- 开发规划；
- 半成品总结。

这会导致它越来越长，也越来越难用。尤其是当项目已经做出很多东西以后，再继续往需求说明书里追加“补充说明”，文档就会越来越像拼接出来的。

所以更合理的做法是把它们拆开：

1. **功能需求说明书**：保留为需求阶段文档，描述最初希望项目具备什么；
2. **项目介绍说明书**：描述当前项目真实做到了什么，它怎么运行；
3. **论文**：在学术和系统设计语境下重新整理这些内容。

这也是我这次新建这份《项目介绍说明书》的原因。

---

## 九、这份说明书后面可以怎么用

这份文档后面最适合拿来做三件事：

1. **给老师或同学介绍项目时直接当讲解底稿**  
它现在已经比较接近“别人第一次听你讲项目时应该听到什么”。

2. **写毕业论文时作为项目事实来源**  
尤其适合拿来支撑论文里的：
   - 项目概况；
   - 系统需求分析；
   - 总体设计；
   - 系统实现；
   - 测试与不足。

3. **继续做功能时拿来判断哪些是核心，哪些是外围**  
这份文档已经刻意把动作战斗、技能时间轴、敌人流程、成长与存档写得更重，把声音设置、菜单等写得更轻，这样后面继续开发时也更容易知道优先级。

---

## 十、结语

当前的《无尽轮回》已经不再是一个只有角色控制的雏形项目，而是一个具备主流程、战斗、成长、交互和存档恢复能力的动作游戏原型。它最有价值的部分，不在于单独某一个按钮或某一张面板，而在于这些系统已经开始连成一个完整闭环：玩家可以进入游戏、推进关卡、获得成长、承受失败、回滚重来，并把这些结果继续带到后续流程里。

当然，它距离“内容量丰富、打磨完整的成品游戏”还有明显距离，但这并不影响它已经具备作为毕业设计项目的完整性和说明价值。对你后面写论文来说，这个阶段最重要的不是再把项目说得多夸张，而是把它现在已经做出来的东西讲清楚、讲准确。  

这份文档就是朝这个方向整理出来的。
