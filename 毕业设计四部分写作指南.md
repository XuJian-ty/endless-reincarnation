# 毕业设计四部分写作指南（针对本 3D 肉鸽游戏项目）

本文档说明如何将毕设任务书中的四个阶段（需求分析及文献检索、系统总体设计、系统实现、撰写及修改毕业论文）**落实到当前游戏项目**，并如何与《功能需求说明书》和现有代码对应。  
**注意**：若学校题目仍是「基于 JSP 的企业门户网站」，需与导师确认是否允许改为「基于 Unity 的 3D  Roguelike 游戏设计与实现」等相近题目，或按「课程设计/大作业」形式用游戏项目替代；以下内容均按**以本游戏为毕设对象**来写。

---

## 一、需求分析及文献检索（约 1 周）

### 1.1 本部分要写什么

- **需求分析**：说明「为什么要做这个系统」「解决什么问题」「谁在用」「核心功能有哪些」。
- **文献检索**：查阅并引用与选题相关的文献（教材、期刊、技术文档、优秀毕设等），在论文中写「国内外研究现状」或「相关技术综述」。

### 1.2 如何对应你的项目

**需求分析**可直接依托《功能需求说明书》：

| 论文中的表述       | 对应需求说明书位置 |
|--------------------|--------------------|
| 项目背景与意义     | 文档说明 + 二十一节「关卡内流程小结」整体描述 |
| 用户角色           | 无管理员；**用户 = 玩家**，需求说明书中所有「玩家可以…」即用户需求 |
| 功能需求列表       | 第一～二十节：开始界面、存档读档、检查点与死亡、关卡与地图、怪物/守卫者/Boss、玩家属性与成长、武器与装备、背包与物品、拾取掉落与宝箱、商店与药水、仙露与天赋点、技能与技能树、按键配置、Buff、角色操作、输入与动作优先级、伤害与战斗、菜单与暂停、数据与配置 |
| 非功能需求         | 可自行补充：可玩性、可维护性（数据驱动）、可扩展性（分层架构）、性能（对象池等） |

**文献检索建议方向**（按你的技术栈和类型来查）：

- **游戏开发与设计**：Roguelike 游戏设计、关卡设计、成长与掉落设计。
- **Unity 与 C#**：Unity 官方文档、Unity 游戏架构、C# 在游戏中的应用。
- **软件工程**：分层架构、状态机、事件驱动、数据驱动设计。
- **可选**：第三人称动作、伤害公式与数值设计、存档系统设计、输入系统与按键重绑定。

在论文里可设小节：「2.1 需求分析」「2.2 国内外研究现状 / 相关技术」；需求分析用「2.1」概括说明 + 引用功能需求说明书中的章节编号（如「参见功能需求说明书第四节 关卡与地图」）。

### 1.3 可输出的成果

- 论文中「需求分析」小节（文字 + 功能列表/用例简述）。
- 文献综述或「相关技术」小节（若干篇文献的归纳与引用）。
- 若学校要求单独交「需求分析报告」，可从功能需求说明书中摘录并补上「项目背景」「用户」「非功能需求」即可。

---

## 二、系统总体设计（约 3 周）

### 2.1 本部分要写什么

- **总体架构**：系统分几层、每层职责、层与层之间如何调用。
- **功能模块设计**：按功能划分子系统（如主菜单、关卡、战斗、背包、存档等），说明模块职责与接口。
- **数据库/数据设计**：若题目是网站则写数据库表结构；**你的项目是游戏**，应写**配置与存档数据结构**（ScriptableObject 配置 + JSON 存档）。
- **界面/流程设计**：主要界面有哪些、核心业务流程（如：主菜单 → 新游戏/读档 → 进关 → Buff 选择 → 游玩 → 死亡/通关/退出）。

### 2.2 如何对应你的项目

**总体架构**可直接引用或改写《README_Architecture.md》：

- 分层：UI 层 → 表现层 → 流程层 → 领域层 → 数据层 → 基础设施层；依赖方向、数据驱动、SO 与运行时分离。
- 设计模式：单例、对象池、观察者/事件、状态机、策略（SO）、模板方法（BasePanel）、组合、DTO、服务定位等；在论文中选 3～5 个重点写（如状态机、事件、SO 数据驱动）。

**功能模块**可按「功能需求说明书」的章节来划分，并对应代码目录：

| 功能模块         | 需求说明书章节           | 代码/资源位置 |
|------------------|---------------------------|----------------|
| 主菜单与流程     | 一、二、十九              | GameFlow（GameStateMachine）、UI（MainMenuPanel、SaveListPanel、NamePanel、LoadingPanel） |
| 存档与读档       | 二                        | Saving（SaveSystem、SaveData、FileSaveStorage）、GameFlow |
| 检查点与死亡     | 三                        | GameFlow（Checkpoint、Boss 进度）、领域层（PlayerModel 等） |
| 关卡与地图       | 四                        | GameFlow（LevelDirector、LevelBootstrapper）、Data（LevelConfigDatabaseSO 等） |
| 怪物与 Boss      | 五                        | Presentation（EnemyController）、Data（EnemyStatsSO/EnemyStatsDatabaseSO）、GameFlow |
| 玩家属性与成长   | 六                        | Domain（Stats、ExpModel、PlayerModel）、Data（LevelGrowthSO） |
| 武器与装备       | 七                        | Domain（EquipmentModel）、Data（WeaponSO、WeaponInstance、WeaponDatabaseSO） |
| 背包与物品       | 八                        | Domain（InventorySlot、PlayerModel 内背包）、UI（BackpackPanel）、Data（ItemDisplayDatabaseSO） |
| 拾取、掉落、宝箱 | 九                        | 表现层 + 流程层 + 配置（DropTableDatabaseSO 等） |
| 商店与药水       | 十                        | Data（ShopPriceConfigSO、PotionConfigSO）、UI、领域层 |
| 仙露与天赋点     | 十一                      | 领域层（PlayerModel）、流程/UI |
| 技能与技能树     | 十二                      | Data（SkillConfigDatabaseSO）、Domain、UI（SkillTreePanel） |
| 按键配置         | 十三                      | Input（GameplayInputEvents）、UI（KeyConfigPanel） |
| Buff             | 十四                      | 领域层（Buff 容器）、配置、进关流程 |
| 角色操作与战斗   | 十五、十六、十七、十八    | Presentation（PlayerController、PlayerStateMachine、各 State、PlayerCombat、DamageDetectionRunner）、Data（AnimationFrameDamageDatabaseSO 等） |
| 菜单与暂停       | 十九                      | UI（MenuPanel、SettingsPanel） |
| 数据与配置       | 二十                      | Data（各类 SO）、Config（ConfigManager）、需求说明书「二十（补充）」 |

**数据设计**（代替「数据库设计」）：

- **配置数据**：列出主要 ScriptableObject（武器库、敌人属性库、关卡配置库、掉落表库、技能配置库、动画帧伤害/特效库、商店价格、药水效果等），说明用途和关键字段；可引用《功能需求说明书》「二十（补充）数据配置文件与数据模型」表格。
- **存档数据**：SaveData / RunData / PlayerSaveData / LevelSnapshot 等；说明存什么（关卡、难度、等级、背包、装备、Boss 进度等），对应需求说明书第二节「存档中保存的内容」。

**界面与流程**：

- 界面：主菜单、起名、存档列表、加载中、关卡内 HUD、背包、技能树、商店、Buff 选择、菜单/暂停、按键配置、战斗回忆等；与需求说明书「一、二、十九」及 UI 预制体对应。
- 流程：用流程图画出「主菜单 → 新游戏/读档 → 进关 → Buff 选择 → 探索/战斗 → 守卫者全灭 → Boss → 死亡/通关/退出」；可结合「二十一、关卡内流程小结」来写。

### 2.3 可输出的成果

- 论文中「系统总体设计」章：总体架构图、分层说明、功能模块图、数据/配置与存档结构说明、主要界面与流程描述。
- 若需要「概要设计说明书」，可从上述内容整理成独立文档。

---

## 三、系统实现（约 4 周）

### 3.1 本部分要写什么

- **开发环境与工具**：Windows、Unity 版本、Visual Studio / Rider、C#、新输入系统等（对应你的实际环境）。
- **关键模块的实现**：选 4～6 个核心模块，每个用「思路 + 关键类/接口 + 关键代码片段或伪代码」说明，并配类图/时序图（若学校要求）。
- **难点与解决**：例如状态机与输入优先级、伤害检测与动画帧事件、存档与检查点回滚、数据驱动与 SO 配置等。

### 3.2 如何对应你的项目

**开发环境**：Windows；Unity（写明版本）；C#；新输入系统（Input System）；可选 URP、Newtonsoft.Json 等。

**建议重点写的实现模块**（与需求说明书和代码一一对应）：

1. **游戏流程与状态**  
   - 对应需求：开始界面、存档、进关、暂停、退出。  
   - 关键类：`GameStateMachine`、`LevelDirector`、`LevelBootstrapper`、`ScenesMgr`。  
   - 可写：状态划分、新游戏/读档/保存/退出的调用链、与场景加载的配合。

2. **存档与检查点**  
   - 对应需求：第二节存档内容、第三节检查点与死亡复活。  
   - 关键类：`SaveSystem`、`SaveData`/`RunData`/`LevelSnapshot`、`ISaveStorage`、`FileSaveStorage`。  
   - 可写：存档槽位、序列化结构、何时写入、检查点与「仙露复活」回滚逻辑。

3. **玩家与战斗数值**  
   - 对应需求：六、七、十八（属性、装备、伤害公式）。  
   - 关键类：`PlayerModel`、`Stats`、`CombatCalculator`、`EquipmentModel`、`WeaponInstance`。  
   - 可写：属性与 Modifier 叠加、伤害公式、装备穿戴与卸下对属性的影响。

4. **角色状态机与输入**  
   - 对应需求：十五、十六、十七（移动、攻击、闪避、优先级、状态表）。  
   - 关键类：`PlayerStateMachine`、`PlayerController`、各 `*State`、`PlayerInputHandler`、`PendingActionData`、`TransitionPolicy`。  
   - 可写：状态列表、打断/缓存/忽略策略（可引用需求说明书「十七（补充）」表格）、预输入与连击。

5. **伤害检测与表现**  
   - 对应需求：十八（伤害公式、动画帧伤害）。  
   - 关键类：`DamageDetectionRunner`、`AnimationFrameDamageDatabaseSO`、动画事件。  
   - 可写：伤害名与配置的对应、检测时机（动画事件）、与 CombatCalculator 的衔接。

6. **背包与物品**  
   - 对应需求：八、九（20 格、武器/堆叠、拾取、出售）。  
   - 关键类：`PlayerModel` 内背包、`InventorySlot`、`BackpackPanel`、物品显示配置。  
   - 可写：槽位结构、装备/出售、满包不拾取与提示。

每个模块写 1～2 页即可：先写「对应需求说明书哪一节」，再写「实现思路与关键类」，最后贴少量关键代码或伪代码并配 1 张图（类图或时序图）。

**难点与解决方案**示例：

- 状态多、输入复杂 → 用状态机 + 策略表（打断/缓存/忽略）+ 预输入槽。
- 伤害与动画同步 → 动画帧事件 + 伤害名 + 配置库（AnimationFrameDamageDatabaseSO）。
- 存档要支持回滚 → 检查点存「Boss 后快照」，死亡复活时用检查点覆盖当前进度并难度+1。

### 3.3 可输出的成果

- 论文中「系统实现」章：环境说明、4～6 个关键模块的实现说明、关键代码与图示、难点与解决。
- 若学校要求「详细设计说明书」，可对上述模块再展开接口与流程说明。

---

## 四、撰写及修改毕业论文（约 6 周）

### 4.1 本部分要写什么

- 按学校模板整理全文：摘要、目录、绪论、需求分析、总体设计、实现、测试（若有）、总结与展望、参考文献。
- 保证与前三部分一致：需求对应功能需求说明书，设计对应架构与模块，实现对应真实代码与类名。
- 定稿、查重、答辩 PPT。

### 4.2 论文章节与前三部分的对应关系

| 论文章节（示例）     | 内容来源 |
|----------------------|----------|
| 第 1 章 绪论         | 背景、意义、题目说明（可改为「基于 Unity 的 3D Roguelike 游戏设计与实现」等）、论文结构 |
| 第 2 章 需求分析     | **第一部分**：功能需求（来自功能需求说明书）、用户角色、非功能需求；可含「相关技术/文献综述」 |
| 第 3 章 系统总体设计 | **第二部分**：总体架构、功能模块、数据/配置与存档设计、界面与流程 |
| 第 4 章 系统实现     | **第三部分**：环境、关键模块实现、关键代码与图示、难点与解决 |
| 第 5 章 测试与运行   | 主要功能测试、典型界面截图、简单测试用例（可选） |
| 第 6 章 总结与展望   | 完成内容总结、不足与后续改进 |
| 参考文献             | 第一部分检索的文献 + Unity/ C# 官方文档等 |

### 4.3 写作时的注意点

- **统一用语**：论文中的术语（检查点、守卫者、Boss、Buff、技能树、背包槽位等）与《功能需求说明书》保持一致。
- **引用文档**：在需求分析、总体设计中可写「详见项目《功能需求说明书》第 X 节」或「见 3.2 节数据设计」。
- **代码与图表**：实现章节中的类名、文件名要与 `Assets/Scripts/Game` 下实际代码一致；架构图、模块图可基于 README_Architecture 和 README_Progress 画。
- **题目若仍为 JSP 网站**：必须与导师沟通，要么改题为游戏，要么用游戏作为「扩展/对比」或「课程设计」，避免论文题目与内容完全不符。

---

## 五、小结：四个阶段与你的项目对照表

| 阶段               | 时间  | 你的项目对应内容 |
|--------------------|-------|------------------|
| 需求分析及文献检索 | 1 周  | 《功能需求说明书》全文 + 文献检索（Unity、Roguelike、架构、状态机等） |
| 系统总体设计       | 3 周  | README_Architecture + 功能模块表 + 数据/配置与存档设计 + 流程与界面 |
| 系统实现           | 4 周  | 开发环境 + 4～6 个关键模块（流程、存档、玩家与战斗、状态机与输入、伤害检测、背包） + 难点与解决 |
| 撰写及修改论文     | 6 周  | 按学校模板组稿、与前三部分一致、定稿与答辩 |

按上述方式写，你的毕设四部分就能**完全围绕当前 3D 肉鸽游戏**展开，并始终与《功能需求说明书》和现有代码对应，避免与「企业门户网站」题目混在一起。若学校有固定模板或章节要求，可在本指南基础上微调章节标题即可。
